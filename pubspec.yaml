name: mazadxx
description: Offline-first auction-first ecommerce app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  get: ^4.6.6
  google_fonts: ^6.2.1
  shared_preferences: ^2.2.3
  flutter_animate: 4.5.2
  iconly: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/onboarding/
    - assets/images/auctions/
    - assets/images/wanted/
    - assets/data/
